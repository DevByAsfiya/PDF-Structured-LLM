# JAL Catalogue Scraper — dependencies
# Install:  pip install -r requirements.txt
#
# torch is NOT listed here. It is already installed from the CPU-only index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Keeping it out avoids the CPU index leaking onto every other package.

# --- Core: PDF parsing and extraction (phases 1-3) ---
pymupdf>=1.24
pdfplumber>=0.11
pillow>=10.4
imagehash>=4.3

# --- Data ---
pandas>=2.2
pyarrow>=17.0
numpy>=1.26

# --- Config, models, CLI ---
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
typer>=0.12
rich>=13.7
loguru>=0.7
jinja2>=3.1

# --- Database (phase 4) ---
sqlalchemy>=2.0

# --- LLM enrichment (phase 4) ---
groq>=0.11
openai>=1.40
tenacity>=9.0
diskcache>=5.6
pyyaml>=6.0

# --- Search (phase 5) ---
sentence-transformers>=3.0
lancedb>=0.13
rank-bm25>=0.2.2

# --- UI (phase 6) ---
streamlit>=1.38
plotly>=5.24

# --- Export (phase 7) ---
openpyxl>=3.1
reportlab>=4.2

# --- Dev ---
pytest>=8.3